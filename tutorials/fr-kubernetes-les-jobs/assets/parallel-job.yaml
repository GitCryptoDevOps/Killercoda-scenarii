apiVersion: batch/v1
kind: Job
metadata:
  name: parallel-job
spec:
  activeDeadlineSeconds: 60
  parallelism: 3
  template:
    spec:
      containers:
      - name: ubuntu
        image: ubuntu
        command: ["ls", "-l"]
      restartPolicy: Never
