apiVersion: batch/v1
kind: Job
metadata:
  name: repeatable-job
spec:
  activeDeadlineSeconds: 60
  completions: 3
  template:
    spec:
      containers:
      - name: ubuntu
        image: ubuntu
        command: ["ls", "-l"]
      restartPolicy: Never
