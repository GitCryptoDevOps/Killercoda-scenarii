apiVersion: v1
kind: Pod
metadata:
  name: healthy-pod
spec:
  containers:
  - name: www
    image: devopstestlab/webserver-liveprobe
    ports:
    - containerPort: 5000
    livenessProbe:
      initialDelaySeconds: 2
      periodSeconds: 5
      httpGet:
        path: /healthy
        port: 5000
