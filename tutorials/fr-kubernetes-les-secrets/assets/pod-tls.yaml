apiVersion: v1
kind: Pod
metadata:
  name: with-secrets
spec:
  containers:
  - name: helloworld
    image: devopstestlab/nginx-helloworld
    volumeMounts:
    - name: tls
      mountPath: /etc/ssl/certs
  volumes:
  - name: tls
    secret:
      secretName: mykey
