Créer deux namespaces `domaine1` et `domaine2` :

`kubectl create namespace domaine1`{{execute}}

`kubectl create namespace domaine2`{{execute}}

Confirmer qu'ils ont bien été créés :

`kubectl get namespaces`{{execute}}
